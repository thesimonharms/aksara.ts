# Fine-tuning / inference deps (no torch — install backend separately).
#
# Intel Arc XPU (Windows eGPU / Battlemage B60):
#   py -3.12 -m venv training/trocr/.venv-xpu
#   .venv-xpu\Scripts\pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/xpu
#   .venv-xpu\Scripts\pip install -r training/trocr/requirements.txt
#
# CUDA:   pip install torch --index-url https://download.pytorch.org/whl/cu124
# CPU:    pip install torch --index-url https://download.pytorch.org/whl/cpu

transformers>=4.46,<5.0
datasets>=2.20
accelerate>=0.34
evaluate>=0.4
sentencepiece>=0.2.0
python-dotenv>=1.0
jiwer>=3.0
pillow>=10.0
PyMuPDF>=1.24
huggingface_hub[hf_xet]>=0.24
